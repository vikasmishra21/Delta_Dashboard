import { ScoreAndBasePipe } from './score-and-base.pipe';

describe('ScoreAndBasePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreAndBasePipe();
    expect(pipe).toBeTruthy();
  });
});
