import { ShowSigTestPipe } from './show-sig-test.pipe';

describe('ShowSigTestPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSigTestPipe();
    expect(pipe).toBeTruthy();
  });
});
